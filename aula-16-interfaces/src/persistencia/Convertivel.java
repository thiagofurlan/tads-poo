package persistencia;

// a interface é totalmente abstrata
public interface Convertivel {
	// todos os métodos de uma interface são
	// por padrão públicos e abstratos
	// ("nunca" tem corpo)
	Object[][] getMatriz();
}
